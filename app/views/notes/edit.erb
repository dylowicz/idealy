<h1>Edit Note</h1>

<%= form_for(@note, url: idea_note_path(@note.idea, @note), method: :patch) do |f| %>
    <p>
      <%= f.label :content %><br>
      <%= f.text_area :content, autofocus: true %>
    </p>

    <p>
      <%= f.submit %>
    </p>
<% end %>

<%= link_to 'Back', idea_path(@idea) %>